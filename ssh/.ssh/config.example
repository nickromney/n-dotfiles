# Base SSH config.
# Keep machine-wide defaults here and put host-specific entries in
# ~/.ssh/config.d/*.conf or grouped one directory deep under
# ~/.ssh/config.d/*/*.conf.
Host *
  IdentityAgent "~/.1password/agent.sock"

Include ~/.ssh/config.d/*.conf
Include ~/.ssh/config.d/*/*.conf
